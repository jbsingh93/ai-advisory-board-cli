---
"ai-advisory-board": patch
---

`aab init` now suggests `aab ui` in the "Next steps" output. The web dashboard at `http://127.0.0.1:3737` is the most welcoming entry point for first-time users, so it's now surfaced right after `aab doctor` with a "(recommended)" hint.
