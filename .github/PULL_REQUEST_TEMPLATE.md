<!-- Thanks for the PR. Keep this short — diff and CHANGELOG entry carry the detail. -->

## What changed

<!-- 1-3 sentences on the user-visible change or refactor. -->

## Why

<!-- One sentence on the motivation. Link an issue if one exists. -->

Closes #

## Verification

<!-- Tick what you actually ran. Don't claim what you didn't. -->

- [ ] `npm run typecheck` passes
- [ ] `npm run build` succeeds
- [ ] `npm run test:run` passes
- [ ] CLI change → live smoke run from external test folder (see `docs/development/SMOKE_TESTING.md`)
- [ ] UI change → Playwright MCP smoke (see `docs/development/PLAYWRIGHT_MCP.md`)
- [ ] CHANGELOG.md entry added (if user-visible)
- [ ] `npx changeset` run (if user-visible — describes the version bump)

## Notes for the reviewer

<!-- Optional: things you tried and discarded, follow-ups to break out, areas you'd like extra eyes on. -->
