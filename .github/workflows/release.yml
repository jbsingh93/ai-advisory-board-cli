name: Release

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: release-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

jobs:
  release:
    name: Release (changesets)
    runs-on: ubuntu-latest

    # Required for:
    #   - id-token: write  → npm trusted publishing (OIDC, no NPM_TOKEN needed)
    #   - contents: write  → changesets/action opens the release PR
    #   - pull-requests: write → changesets/action updates the release PR
    permissions:
      id-token: write
      contents: write
      pull-requests: write

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      # Node 24 (not 22) because trusted publishing requires npm CLI 11.5.1+.
      # Node 22.x ships with npm 10.x and can't actually use OIDC even when
      # available — the publish fails with a misleading "E404 Not Found".
      # Node 24.x ships with npm 11.x bundled. The package itself supports
      # Node 20+ (verified by the CI matrix on the ci.yml workflow); Node 24
      # here is just the build/publish runtime.
      - name: Set up Node 24
        uses: actions/setup-node@v7
        with:
          node-version: 24
          cache: npm
          registry-url: https://registry.npmjs.org

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build

      - name: Create release PR or publish
        uses: changesets/action@v1
        with:
          # When the release PR is merged, this script runs `changeset publish`
          # which publishes to npm. Trusted publishing via OIDC means no
          # NPM_TOKEN is needed — npm verifies the GitHub Actions workflow
          # identity via the id-token permission above.
          publish: npm run changeset:publish
          version: npm run changeset:version
          commit: "chore: release"
          title: "chore: release"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
